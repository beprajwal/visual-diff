{
  "name": "vdiff-fixture-store",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Tiny Vite storefront used as the visual-diff fixture app and demo (spec §11.2).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
