{
  "name": "vdiff-fixture-weather",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Weather dashboard over Open-Meteo — the visual-diff fixture app with a real recorded API (api-mocking spec §9), and the source of the README screenshots.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "fixture:record": "node scripts/record-har.mjs"
  },
  "dependencies": {
    "preact": "^10.25.4"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
