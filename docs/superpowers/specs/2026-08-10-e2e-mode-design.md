# E2E Mode — Design (Subsystem 4: ingesting an existing test suite)

Date: 2026-08-10
Status: Approved for planning
Builds on: slice 1 (D1–D9), API mocking (D10–D14), variants (D20–D24)

## 1. Problem

Teams with an end-to-end suite already drive their application through its real workflows, in CI, on
every change. That is a great deal of coverage the visual-diff tool cannot see: flows are authored
separately, replayed separately, and the two never meet.

E2E mode ingests what an existing suite already produces — Playwright traces — and turns each test
into a run the store, diff engine and report can handle. No plugin, no configuration change, no
dependency added to their project.

It is **explicitly opt-in**, per the original framing. Nothing about `vdiff run` changes; e2e is a
separate command over artifacts that already exist.

## 2. Scope

### In

- `vdiff e2e --from trace <path>`: ingest Playwright trace archives into runs
- Mapping tests to flows and trace steps to step ids, with an override file
- E2E runs badged, kept on their own timeline, with noise-tolerant diff defaults

### Explicit non-goals

- **We do not run their tests.** No shelling out to their test command, no responsibility for their
  exit codes, retries, sharding or CI wiring.
- **No plugin or reporter.** Nothing is added to their `playwright.config.ts`, and no package of
  ours enters their dependency tree.
- **Playwright traces only.** Cypress, WebdriverIO and others are out of scope. The ingestion layer
  stays format-agnostic internally so a second reader can be added without reshaping the pipeline,
  but no second reader ships here.
- **No determinism guarantees.** Their tests control the browser; none of the runner's knobs apply.
  See §5.
- **No scenarios or variants over e2e runs.** Both operate during capture, and e2e capture already
  happened.

## 3. Decisions and rationale

Continues the record: slice 1 D1–D9, mocking D10–D14, harness packaging D15–D19, variants D20–D24.

**D25 — Ingest artifacts; never drive the suite.**
Their suite runs however it already runs, with tracing enabled; we read the output. Rejected: a
published Playwright reporter (higher fidelity and proper settle-gate control, but it is a config
change and a package in their dependency tree, coupled to a framework plugin API that moves) and
shelling out to their test command with injected instrumentation (makes us responsible for their
runner's behaviour, and in practice only works for Playwright anyway — the same constraint, bought
at a much higher price).

**D26 — Test titles map to flows; trace step titles map to step ids.**
Diffing depends on stable step ids (D4). Traces offer titles and action boundaries, neither designed
as identifiers, so titles are normalized, collisions disambiguated with a stable suffix, and
`.visual-diff/e2e-map.yaml` pins a title to an id for suites that rename often. Rejected: annotating
their tests to declare ids (stable by construction, but it is a change to their repo, which is the
thing D25 exists to avoid) and ordinal positions (inserting one assertion shifts every subsequent
step and misaligns the entire diff — the failure D4 rejected for flows).

When a title does drift, the step appears as removed-and-added. Visible, rather than silently
compared against the wrong screen.

**D27 — E2E runs are a separate, noise-tolerant timeline.**
Badged, paired only with other e2e runs by default, with their own higher pixel threshold and larger
minimum region area. Cross-pairing an e2e run with a replay run is permitted and flagged at high
severity, exactly as mock-versus-recorded is (D13). Rejected: putting e2e runs through the standard
pipeline with only a label, which would greet a first-time user with a wall of findings generated by
animation frames and clock text — and the credibility of a diffing tool is set entirely by its
false-positive rate.

**The thresholds in §5 are provisional.** They were chosen without a real-world suite to calibrate
against and are expected to change. They are recorded as tunable configuration, not as constants,
so they do not silently become folklore.

## 4. What a trace yields, and what it does not

A Playwright trace archive contains action boundaries, DOM snapshots, screenshots and network
activity. That covers most of what slice 1 captures — with one material gap.

| slice 1 capture | from a trace |
|---|---|
| screenshot | yes |
| DOM structure | yes, from the snapshot |
| **computed-style subset** | **no** |
| accessibility tree | no |
| console | partially, when the trace records it |
| network | yes |

**Consequence, stated plainly because it would otherwise be discovered as a disappointment:** the
layered diff (D5) degrades for e2e runs. Pixel regions and DOM attribution still work — "this region
changed, this element is responsible" — but property-level findings do not. An e2e diff will not say
"padding 8px → 12px". `findings.json` marks these runs so the report can explain the reduced detail
rather than appear to have missed something.

## 5. Noise handling

E2E capture has no frozen clock, no seeded RNG, no disabled animations and no settle gate. Two runs
of an unchanged application will differ.

Provisional defaults, overridable per project under `e2e:` in config:

| setting | replay default | e2e default |
|---|---|---|
| pixel threshold | standard | higher |
| minimum region area | 64 | larger |
| antialias tolerance | standard | higher |

Masking matters more here than anywhere else in the tool, so `e2e-map.yaml` also accepts an `ignore`
list applied to ingested runs — clocks, ids and animated regions, which no amount of threshold
tuning handles correctly.

A rejected alternative worth recording: capturing each shot twice and treating pixels that differ
between the pair as inherently unstable, masking them automatically. It self-calibrates instead of
guessing thresholds, and it is impossible under D25 — the trace is already recorded and we cannot
ask for a second shot. It would become available if we ever adopted a reporter, and is the strongest
argument for revisiting D25 later.

## 6. CLI

```
vdiff e2e --from trace <path|glob>      ingest one or many trace archives
vdiff e2e --from trace <path> --flow <name>    override the derived flow name
vdiff e2e list                          show what would be ingested, without writing
vdiff runs <flow> --e2e                 e2e timeline for a flow
vdiff diff <flow> [base] [head]         pairs e2e with e2e by default
```

`--json` throughout. Ingestion is idempotent: the same trace ingested twice produces one run, keyed
by a content hash of the archive.

## 7. Run identity and store

`meta.json` gains `source: 'replay' | 'e2e'`, defaulting to `replay`. E2E runs record the trace hash,
the originating test title, and the suite's own metadata (browser, project name, retry index) where
the trace provides it.

Retention is a separate bucket, as with variants (D24), so ingesting a CI run's worth of traces
cannot evict replay history.

Revision attribution comes from the trace's git metadata when present; otherwise the run records
`revision: unknown`, which is honest and prevents an e2e run being silently attributed to whatever
happens to be checked out locally.

## 8. Validation and errors

| Situation | Behaviour |
|---|---|
| path is not a readable trace archive | exit 2, naming the file |
| trace contains no screenshots | exit 2 — nothing to diff, and a run with no shots is worse than none |
| trace format version unsupported | exit 2, naming the version and the versions supported |
| duplicate step titles within a test | disambiguated with a stable suffix; reported once as a notice |
| `e2e-map.yaml` references a title no trace contains | run warning listing them — a stale map entry silently doing nothing is the same failure as a never-matched scenario rule |
| no git metadata in trace | `revision: unknown`, recorded, never inferred |

## 9. Testing

1. **Fixture traces committed to the repo.** Generated once from the fixture app's own Playwright
   suite, then committed, so ingestion is tested hermetically and never needs a live suite. This is
   the largest item in the slice.
2. **Ingestion is idempotent** — the same archive twice yields one run.
3. **Title mapping**: normalization, collision disambiguation, `e2e-map.yaml` overrides, and the
   removed-and-added behaviour when a title changes between traces.
4. **Degraded-diff labelling** — e2e findings are marked as lacking property-level detail, and the
   report explains it rather than appearing incomplete.
5. **Pairing** — e2e pairs with e2e by default; e2e-versus-replay is flagged at high severity.
6. **Retention isolation** — ingesting many traces never evicts replay runs.
7. **Noise defaults** applied only to e2e runs, and overridable from config.
8. **Unsupported trace version** produces the §8 error rather than a partial ingest.
9. **CLI `--json` contracts** for `e2e` and `e2e list`.

## 10. Open for implementation

- The exact provisional threshold values (§5), to be set from the fixture suite's own noise floor and
  documented as measured rather than chosen.
- Which Playwright trace format versions to support, and the policy when a newer one appears.
- Whether console entries present in some traces are worth surfacing as `console` findings, given
  they will be inconsistently available.

## 11. Roadmap position

This is the last planned subsystem. With slice 1 shipped and subsystems 1–4 specified, the remaining
work is implementation, plus two items deferred out of earlier slices: `--scenario all` and scenario
composition (mocking §12).
